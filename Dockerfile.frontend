FROM node:22-alpine

WORKDIR /app

# Install dependencies first (caching)
COPY package.json package-lock.json ./
RUN npm install

# Copy source
COPY . .

# Expose Vite port
EXPOSE 5173

# Start Dev Server
CMD ["npm", "run", "dev", "--", "--host"]
